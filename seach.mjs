//Acc to se6 module .mjs is extension
export const linearSearch = (arr, target) => {
  for (let element in arr) {
    if (element == target) return true;
  }
  return false;
};

export const binarySearch = (arr, target) => {
  return true;
};
